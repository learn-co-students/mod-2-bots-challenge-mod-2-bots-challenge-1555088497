<h1>Create Robot</h1>

<%= form_for(@robot) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :ability %>
<%= f.collection_select :ability_id, @abilities, :id, :name %>
<%= f.submit "Add" %>
<% end %>
